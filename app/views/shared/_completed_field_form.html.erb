<%= form_for task, :remote => true do |f| %>
  <%= f.hidden_field :completed, :value => !task.completed %>
  <% if task.completed? %>
    <%= f.submit "Not okay!", :class => 'undone' %>
  <% else %>
    <%= f.submit "Done!", :class => 'done' %>
  <% end %>
<% end %>
