<%= form_for task, :remote => true do |f| %>
  <%= f.hidden_field :completed, :value => !task.completed %>
  <% if task.completed? %>
    <%= f.submit "Not okay!" %>
  <% else %>
    <%= f.submit "Done!" %>
  <% end %>
<% end %>
