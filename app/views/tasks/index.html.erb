<h1>Tasks</h1>

<% if @tasks.empty? %>
  <p>No tasks found.</p>
<% else %>
  <%= render :partial => 'shared/tasks', :collecion => @tasks %>
<% end %>

<p>
  <%= link_to "New task", new_task_path %>
</p>
