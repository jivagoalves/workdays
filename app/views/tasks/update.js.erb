$("#completed_field_form_<%= @task.id %>").html("<%= escape_javascript(render :partial => 'shared/completed_field_form',
                                                                              :locals => { :task => @task }) %>");

